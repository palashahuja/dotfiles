alias mvpr="mkdir -p frontend; mkdir -p backend; cp -rf frontend.go views frontend; cp backend.go backend"
alias stsf="go build; ./frontend --listen 8900 --backend 127.0.0.1:9000"
alias stsb="go build; ./backend --listen 9000"
